// import { USER_URL } from "../constants";
// import { apiSlice } from "./apiSlice"; //  endpoints: (builder) => ({}),

// export const userApiSlice = apiSlice.injectEndpoints({
//     endpoints: (builder) => ({
//         login: builder.mutation({
//             query: () => {
//                 url: `${USER_URL}/auth`,
//                     method: 'POST',
//                         body:data
//           }
//       })
//   }),
// });

// export const {} = userApiSlice;
